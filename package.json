{
  "name": "voice-satellite-card-integration",
  "version": "6.1.5",
  "description": "Voice Satellite for Home Assistant — turns browsers into Assist satellites.",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/sync-version.js && node scripts/copy-tflite.js",
    "build": "webpack --mode production",
    "predev": "node scripts/sync-version.js && node scripts/copy-tflite.js",
    "dev": "webpack --mode development && xcopy /E /I /Y custom_components\\voice_satellite \\\\hassio\\config\\custom_components\\voice_satellite"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "@tensorflow/tfjs-core": "^4.9.0",
    "@tensorflow/tfjs-tflite": "^0.0.1-alpha.10",
    "babel-loader": "^9.1.3",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
