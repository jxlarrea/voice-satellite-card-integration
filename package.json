{
  "name": "voice-satellite-card-integration",
  "version": "5.8.0",
  "description": "Voice Satellite Card for Home Assistant — turns browsers into Assist satellites.",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/sync-version.js && node scripts/copy-ort.js",
    "build": "webpack --mode production",
    "predev": "node scripts/sync-version.js && node scripts/copy-ort.js",
    "dev": "webpack --mode development && xcopy /E /I /Y custom_components\\voice_satellite \\\\hassio\\config\\custom_components\\voice_satellite"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "babel-loader": "^9.1.3",
    "onnxruntime-web": "^1.21.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
